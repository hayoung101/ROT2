# 로봇 가구 LLM Agent 프로젝트 — 컨텍스트 프롬프트 (v4.4)

너는 이 프로젝트의 개발을 돕는 조수야. 아래는 이미 확정된 설계 내용이니, 이를 전제로 대화를 이어가.

> v3 → v4 주요 변경: **VLM 시각 자가검증(critic) 전면 제거**, **HITL-1 실제 블로킹 승인 구현**, **HITL-2 승인 시 코드가 자동 commit**, **revert를 main.py에서 결정론적으로 처리(의도층이 revert_to_turn 지정)**, **되묻기(clarification)를 형태층 tool에서 의도층+HITL 앞단으로 이관(tool 13→12개)**, **store_robot no-op 가드**, **check_feasibility issues에 수정 힌트**, **find_placement에 panel_toward_anchor 추가**, **시작 시 resume 제거(재시작=도크 초기화)**, **뷰어 중복 말풍선·GLB 스왑 race 수정**, **render.py 제거**.
>
> v4.1 패치: **find_placement에 connect 모드**, **transform/move 직후 자동 validate_layout**, **연결 자동 감지**, **revert 대상 = '현재 상태와 다른' 가장 최근 커밋**, **commit_if_changed가 (entry, changed) 반환**, **HITL 대기 중 재접속 시 pending 요청 재전송**, **되묻기 최대 2회 후 LLM이 잔여 정보 추론해 진행**, **handle() 예외 격리**, **get_recent_context 슬림화**, **GLB 스왑 후 inactive dim 재적용**, **stt.py에서 콘솔·Windows 경로 제거**, **tests/ 제거**, **panel_away_from_anchor 추가**, **move_robot 반환에 panel_orientation**, **형태층 마무리 발화 채팅 미표시**, **성인/아이 구분 제거**, **기능층 독립(ask_function)**, **scenes 가구에 능력 description 추가**, **V자 골·∧자 지붕을 각도 목록의 고정 케이스로 명시**.
>
> v4.2 변경: 논문 방향을 VoicePilot식 구조로 확정, 자율성 3수준 비교 실험 계획, baseline을 별도 폴더로 분리, 이벤트 궤적 전체 로깅.
>
> **v4.4 변경 (씨앗 평가 요소 재정의 — 15.2·15.4·17절):**
> **(1) 씨앗 평가 요소를 3개에서 2개로 축소 — M1 의도 가독성 / M2 맥락 적합성. 구 M3(맥락 적합성)가 M2로 번호 이동.**
> **(2) 구 M2(행동 방해)를 씨앗에서 제외. 선행연구가 측정한 것은 로봇의 물리적 변화가 진행 중인 신체 활동을 침범하는 것인데, 시뮬레이션에는 사람이 없으므로 직접 관측할 수 없다. 한계로 명시(15.2·18절).**
> **(3) 공간 침범 관련 상태량(변형 전후 footprint 증가량 등)을 M2 기하 지표로 흡수.**
> **(4) 개입·대기·포기 로그를 「상호작용 마찰(IF)」로 분리. 씨앗이 아니라 시스템 품질 부수 지표이며 NASA-TLX와 같은 위치.**
> **(5) TransformTable 3문항은 IF의 자기보고로 유지하되, 원 척도(물리 변형에 대한 반응)와의 직접 수치 비교는 하지 않는다.**
>
> **v4.3 변경 (논문 방향 전면 재설정 — 15·16·17절 전면 개정):**
> **(1) 논문 주제가 "자율성 비교"에서 "사용자 행동에서 평가 기준을 생성하는 프레임워크"로 바뀜.**
> **(2) 실험 조건이 3개(수동/발화/혼합)에서 2개(C1 관찰·설문 / C3 참여·실시간프로빙)로 축소. 조작 방식은 독립변수가 아니라 통제변수.**
> **(3) 본 시스템에 자율 동작이 없음을 명시(HITL-1 블로킹 + HITL-2 승인 필수). 시스템 주도 변화는 연구 범위 밖.**
> **(4) baseline 별도 모듈 분리를 폐기. 조작 UI(드래그·회전·패널 버튼)를 본 시스템 뷰어로 통합(16절).**
> **(5) 리플레이 재생 모드 신설 — C1 조건의 자극 제시용(16절).**
> **(6) 평가 층 신설 — 이벤트 수집·후보 탐지·정규화·측정가능성 판정·프로빙·소급 계산(17절).**
> **(7) 사람 배치 수집 사전 연구(구 실험 A) 삭제.**
> **(8) session.json 로그 스키마 대폭 확장(17.6).**

## 1. 프로젝트 개요

사용자의 **음성 발화**를 듣고, 변신 로봇 가구(shape-shifting robot furniture)를 알맞은 형태·위치로 구성해주는 LLM agent 시스템.
목적: 사용자가 "친구랑 밥 먹을 거야"라고 말하면 시스템이 상황을 파악해 로봇들을 알맞게 변형·배치하고, 사용자 피드백으로 수정하는 것.

- 기반 코드: STT(브라우저 push-to-talk + Groq Whisper) → OpenAI LLM 의도분석 → 로봇 명령 JSON 출력 (structured outputs, strict json_schema)
- **로봇은 BoT²로 확정** (KIST, 모듈형 로봇 가구. 단일 기종 2대: BOT 1, BOT 2). 상세 스펙은 12절.
- 사용자는 성인/아이를 구분하지 않는다.
- **음성 입력은 브라우저 push-to-talk**: 채팅창 🎤 버튼 또는 스페이스바를 누른 채 말하기 → MediaRecorder → POST /stt → Groq Whisper. ROS2 변환은 사용자가 명시적으로 "ROS로 바꿔줘"라고 할 때만 진행.
- **배치의 '조화' 판단은 LLM 자가 점검 + HITL-2 몫**이다.
- **v4.3 추가**: 이 시스템은 구성 도구인 동시에 **평가 관측 장치**다. HITL 게이트·기능층 feasible 판정·절대 스냅샷 커밋은 원래 다른 목적으로 설계됐지만, 평가 신호의 계측점으로 재사용된다(17절).

## 2. 핵심 설계 원칙 (차별점)

1. **3층 변환 구조**: 발화 → [의도층] intent → [기능층] 로봇 무관 가구 요구 목록 → [형태층] 로봇 구성(형태·위치). 기능층은 별도 LLM 호출(ask_function)로 독립. 의도층은 가구 초안만 내고, 기능층이 기존 가구 description을 근거로 중복·보완을 판단하고 각 항목의 구현 가능성(feasible)을 판정한다. HITL-1 승인 후, intent_type이 new_scene/add일 때만 실행.
2. **2중 human-in-the-loop**: HITL-1 언어 게이트(의도 확인 — **분석된 의도를 사용자에게 블로킹 승인받은 뒤에야 형태층 진행**), HITL-2 공간 게이트(3D 뷰어로 배치 확인 — **승인 즉시 코드가 스냅샷을 자동 commit**).
3. **상태 편집 + 버전 복원**: 대화는 버전된 history를 만들고, LLM은 현재 상태의 편집기. "원래대로"는 재생성이 아니라 저장된 state의 결정론적 **복원**.
4. **LLM 제안 / 코드 보장**: LLM은 배치를 제안만 하고, 충돌·경계·연결 기하 검증은 결정론적 코드 레이어가 최종 책임. **좌표·패널 방향 선택도 코드가 계산해 후보로 주고(find_placement) LLM은 고르기만 한다.**
5. **카탈로그가 아닌 문법**: 프롬프트(ROBOT_MECHANISM)는 물리적 사실만 서술. 가구 이름·용도 매핑은 furniture_motifs.json에 reference로만 둠.
6. 환경은 scene JSON으로 표현 (고정 실험 제약 없음).
7. **v4.3 추가 — 평가 층에도 같은 분업**: 평가 후보의 **탐지는 결정론 규칙**, **정규화·계산식 생성은 LLM**, **계산식 검증은 코드**, **최종 확정은 사용자**. 연구자는 새 평가 요소의 내용에 개입하지 않는다(17절).
8. **v4.3 추가 — 자율 동작 없음**: HITL-1이 블로킹이고 HITL-2 승인 없이는 커밋되지 않으므로, 모든 상태 변화는 사용자 요청에서 시작해 사용자 승인으로 끝난다. 로봇이 스스로 움직이는 경로는 존재하지 않는다.

## 3. 의도 스키마 (의도층 출력, structured outputs strict)

```json
{
  "number": 2,              // nullable. 인원 단서가 전혀 없으면 반드시 null. null이면 needs_clarification 트리거
  "situation": "친구와 식사하려는 상황",
  "activity": "식사",
  "space": "kitchen",       // living_room/bedroom/kitchen/bathroom/balcony/unknown
  "furniture": [{"item": "식탁", "count": 1}, {"item": "의자", "count": 2}],  // 초안 — 확정은 기능층
  "posture": "sitting",     // standing/sitting/lying
  "intent_type": "new_scene",  // confirm / modify / add / remove / revert / new_scene
  "revert_to_turn": null,   // nullable int
  "needs_clarification": false,
  "clarification_question": null,
  "confirmation_message": "두 분이 함께 식사하시는군요, 준비를 시작할게요."  // HITL-1용. 가구 이름 언급 금지
}
```

**기능층(ask_function)**: HITL-1 승인 후 new_scene/add에서 실행. 입력 = intent(초안) + 방 기존 가구(description) + motifs + ROBOT_MECHANISM. 출력 = `{"furniture": [{item, count, motif, feasible, reason}], "complement_note": str|null}`. feasible 항목이 intent["furniture"]로 교체되고, 보완 이유만 형태층에 전달돼 HITL-2 메시지에서 고지.

> **v4.3 변경**: 구현 불가 항목을 **콘솔 로그만 남기고 버리던 것을 폐기**한다. feasible=false 항목은 원 발화·item·reason과 함께 **session.json에 영구 기록**한다. 이것이 평가 층 T1 트리거의 전제다(17.2).

## 4. intent_type 라우팅

- confirm → 스냅샷 확정. HITL-2 승인 시 이미 자동 commit되므로 변화 있을 때만 새로 commit(commit_if_changed).
- modify/add/remove → 형태층만 재실행. **최소 편집**: 요청 무관 로봇은 유지, 놀고 있는 자원부터 활용.
- revert → **main.py에서 결정론 처리**. `revert_to_turn`으로 `revert_to(turn)`만 호출, 형태층 스킵. fallback: 대상 null/무변화면 '현재와 다른 가장 최근 커밋' 선택.
- new_scene → 의도층부터 전체 재실행. space 바뀌면 방 전환. 새 구성에 안 쓰이는 active 로봇만 store_robot으로 정리.

**되묻기는 HITL 앞단에서 처리**: needs_clarification=true면 main이 HITL-1 전에 되묻고(최대 2회) 답을 발화에 보태 재분석.

## 5. history 형식

```python
history = [
  {"turn": 1, "intent_type": "new_scene", "space": "living_room",
   "utterance": "테이블 만들어줘", "description": "BOT 1 테이블 배치",
   "source": "utterance",          # v4.3 추가: utterance | ui
   "state": {로봇 전체의 절대 상태}},
]
```

- turn은 전역 번호, state는 항상 전체 로봇의 절대 스냅샷(diff 아님) → 단독 복원 가능.
- `commit_if_changed`: 직전 커밋 이후 변화 없으면 재커밋 안 함. 반환은 `(entry, changed)`.
- intent_type new_scene 항목이 상황 경계. space도 기록. commit마다 logs/session.json 저장.

> **v4.3 추가 — 절대 스냅샷의 두 번째 용도**: diff가 아니라 절대 스냅샷이므로 **임의 시점 상태를 사후에 재계산할 수 있다.** 연구 후반에 정의된 지표를 앞선 모든 참가자·모든 턴에 소급 적용하는 것이 가능하며, 이것이 평가 프레임워크의 방법론적 핵심이다(17.5). 따라서 **지표 값을 커밋 시점에 계산해 저장하면 안 된다.** 저장 대상은 언제나 상태 스냅샷이고, 지표는 필요할 때 순수 함수로 산출한다.

> **v4.3 추가 — UI 조작도 turn을 만든다**: 마지막 UI 조작 후 2초간 추가 조작이 없으면 그 묶음을 하나의 turn으로 확정한다. 발화 turn과 동일한 스키마를 쓰되 `source: "ui"`로 구분한다. 이것이 없으면 수정 연쇄 길이와 반복 조정 축을 셀 수 없고, 두 입력 채널이 하나의 타임라인에 들어오지 않는다.

## 6. Tool 목록 (12개, 전부 tools/ 폴더에 정의)

### placement_tools.py (6)
- `transform_robot(robot, panel_left, panel_right, furniture)` — 로봇 변형. 두 패널 각도(0/45/90/135/180)로만. 실행 직후 자동 push_state + 자동 validate_layout.
- `move_robot(robot, x, y, rot)` — 이동 + 회전. 실행 직후 자동 검증. 반환에 `panel_orientation`.
- `store_robot(robot)` — 홈 도크 복귀 + 초기화. 이미 inactive면 no-op.
- `check_feasibility(robots, connections)` — 물리 검증 + 연결 기하. 반환 `{"feasible", "issues"}`, 각 issue에 fix 힌트.
- `find_placement(footprint_radius, near?, avoid[], connect?, ...)` — 유효 후보 좌표 제안. connect 모드: 두 대 조합 정밀 연결 좌표를 코드가 계산.
- `furniture_mapping(activity)` — 활동→가구 조합 참고표. 강제 아닌 reference.

### context_tools.py (5)
- `robot_states()` / `get_environment()` / `get_recent_context(n)` / `commit_layout(description)` / `revert_to(version)`.

### viewer_tools.py (1)
- `ask_user(message)` — HITL-2 게이트. 승인 시 코드가 자동 commit.

주의: 뷰어 갱신은 tool 아님(상태 변경 직후 코드가 자동 push_state). 되묻기는 tool 아니라 의도층 신호로 main이 HITL 앞단 처리.

> **v4.3 확인 필요 [미정]**: `get_recent_context`에 **UI 조작 turn이 포함되는지** 코드 확인이 필요하다. 포함되지 않으면 사용자가 손으로 옮긴 뒤 "좀 더 넓게"라고 말할 때 LLM이 옛 상태를 보게 된다.

## 7. tools/ vs services/ 구분 원칙

- **tools/** = LLM에게 보이는 껍데기(함수+스키마). 내용은 services 호출 한두 줄.
- **services/** = 순수 계산·상태. LLM 없이 테스트 가능. 여러 tool이 공유.
- LLM 호출 가능한 건 registry.TOOLS의 12개뿐. agent.py는 중계자.
- **v4.3**: 평가 층의 지표 산출·후보 탐지 모듈도 **services/에 둔다.** 순수 함수여야 소급 계산이 성립하고, LLM 없이 단독 테스트가 가능하다.

## 8. 프로젝트 구조

```
project/
├── main.py                # 조립 + 단일 발화 루프 + 라우팅. HITL-1 블로킹 + 되묻기 + 기능층 호출 + revert 결정론
├── agent.py               # ask_intent(의도층) + ask_function(기능층) + run_agent(형태층 tool 루프)
├── config.py              # API 키 + 모델명 + 로봇 물리 상수 (단일 출처)
├── prompts.py             # ROBOT_MECHANISM + INTENT_PROMPT + INTENT_SCHEMA + AGENT_PROMPT
├── data/furniture_motifs.json
├── scenes/                # living_room/bedroom/kitchen/bathroom/balcony.json
├── tools/                 # registry.py + __init__.py + placement_tools.py + context_tools.py + viewer_tools.py
├── services/              # placement.py + collision.py + scene.py + stt.py
│   ├── metrics.py         # ★v4.3 신설 — state → 기하 지표 (순수 함수)
│   ├── detectors.py       # ★v4.3 신설 — 후보 탐지 규칙 T1~T9 (결정론)
│   └── criteria.py        # ★v4.3 신설 — 정규화·계산식 검증·코드북 관리
├── eval/                  # ★v4.3 신설
│   ├── normalize.py       # LLM 호출: 후보 발화 → 슬롯
│   ├── mapping.py         # LLM 호출: 슬롯 → 계산식 + 코드 검증
│   ├── probe.py           # LLM 호출: 확인 질문 생성 (설명→확인→근거)
│   ├── retro.py           # 확정 지표의 소급 계산 배치
│   └── codebook.json      # 이중 장부 (fixed / cumulative)
├── viewer/                # popup_viewer.py(FastAPI+WS+POST /stt) + static/index.html + static/viewer.js
│   └── (★v4.3) 조작 UI 통합 + 리플레이 모드
└── models/robot_<L>x<R>.glb (25개, 5×5) + raw/ + 가구 GLB(선택)
```

의존 방향(한 방향): main → agent → tools → services, tools → viewer(push만). services는 tools를 모름.
**v4.3 추가**: eval/은 이벤트를 **구독**만 하고 구성 파이프라인을 호출하지 않는다. 평가 층을 꺼도 시스템이 정상 동작해야 한다.

## 9. 뷰어 아키텍처

- 파이썬(두뇌) ↔ 브라우저 three.js(화면)는 WebSocket. 파이썬 push, 브라우저는 받은 대로 그리고 애니메이션(rot은 최단 경로).
- 메시지: state_update / scene_change / chat / message / approval_request / clarify_request / user_feedback / clarify_answer / user_utterance.
  - **v4.3 추가**: `ui_action`(조작 이벤트 상향) / `replay_frame`(리플레이 재생) / `probe_request`·`probe_answer`(확인 프로빙)
- 중복 말풍선 방지: approval/clarify 문구는 그쪽이 말풍선을 그리므로 chat으로 또 안 보냄.
- 재접속 시 현재 scene+state 스냅샷 push(F5 복구). HITL 대기 중이면 pending 요청도 재전송(req_id 중복 방지).
- 좌표계: scene (x,y) cm. three.js X=x−w/2, Z=d/2−y, rotation.y=rad(rot). collision.py 규약과 일치(검증됨).
- 로봇 에셋: 패널 상태별 GLB 25개, 상태 변경 시 모델 스왑(최신-키 가드 + 실패 시 조립식 fallback). 충돌은 항상 JSON w,d 사각형 기준.

> **v4.3 — 뷰어 역할 확대**: v4.2까지 뷰어는 "그리기만" 했다. v4.3부터 뷰어는 **조작 입력**과 **리플레이 재생**도 담당한다(16절). 단 물리 판정은 여전히 파이썬 collision.py가 최종 책임을 지며, 브라우저는 조작 의도를 상향 전송하고 결과를 받아 그린다.

## 10. 다중 방(scene) 전환

- 방마다 scenes/<space>.json 하나(5개). living_room 400×400 / kitchen 400×250 / bedroom 300×300 / bathroom 200×200 / balcony 400×100.
- 도크: scene JSON "dock" 필드. 없으면 config.home_for() 자동.
- 기존 가구(pre_existing_furniture): `{"id","label","x","y","w","d","rot","corners","model"?}`.
- **기존 가구는 장애물이자 배치 앵커**. 원칙: "중복하지 말되 반드시 기여하라".
- 의도층이 space 추론, 애매하면 unknown → needs_clarification.

> **v4.3 — 실험용 고정**: 본 실험은 **거실(400×400) 하나만** 사용하고 **기존 가구 배치도 scene JSON으로 고정**한다. 방 기하가 상수여야 참가자 간 기하 지표 비교가 성립한다. 다른 방은 튜토리얼용으로만 쓴다.

## 11. 구현 순서 및 현황

1. ~~의도층 파이프라인~~ ✅  2. ~~services 층~~ ✅  3. ~~tools/ + run_agent~~ ✅
4. ~~시각 자가검증(VLM critic)~~ — 제거됨(13절)
5. ~~viewer/~~ ✅  6. ~~기능층 독립(ask_function)~~ ✅
7. ~~baseline 별도 모듈 분리~~ — **v4.3에서 폐기**. 조작 UI를 본 뷰어로 통합하는 것으로 대체(16절)
8. **남은 작업은 14절 TODO 참조**

## 12. 로봇 확정 스펙 (BoT²)

### 물리 스펙
- **구성**: 동일 기종 2대(BOT 1, BOT 2). **본체 크기 불변(size 개념 없음).**
- **본체**: 40×40×50 cm. 윗면 평평(40×40). 바퀴 이동 + 제자리 회전. 방향성 있음 → rot 필수. **높이 50cm 고정.**
- **가동 패널 2개**: 마주보는 두 측면 윗모서리 힌지, 40(폭)×30(길이) cm. 각각 0/45/90/135/180° **5단계**:
  - 0°=닫힘 / 45°=\ 아래 / 90°=수평(상판 30cm 확장) / 135°=/ 위 / 180°=수직(꼭대기 80cm)
- 수납: 패널 열면 서랍장. 고정 패널 2개: 나머지 두 측면.
- 조합: **최대 2대.** 나란히(rot 동일) / 마주보고 패널 맞대기(rot 차 180°).

> **v4.3 — 능력 경계 목록 (평가 층이 추적하는 대상)**: 크기 불변 / 높이 50cm 고정 / 패널 5단계 이산 / 패널 40×30cm 고정 / 최대 2대 / 조합 2가지 / 회전 45° 스텝. **새 평가 요소는 이 경계 근처에서 발생할 것으로 예측되며, 이 예측은 사전 등록한다**(논문 RQ4).

### state 스키마
```json
{"robot": "BOT 1", "active": "active", "x": 180, "y": 140, "rot": 90,
 "panel_left": 90, "panel_right": 0}
```
panel 값은 {0,45,90,135,180}만. inactive도 도크 바닥 차지 → 충돌 포함.

### collision.py (구현 완료)
- OBB(SAT) 충돌. footprint = 본체 40×40 + 돌출 패널. 패널 바닥 돌출 = 30×sin(각도). 1대 풀 확장 상판 40×100cm.
- slack 2cm. panels_touching(끝 맞닿음 + 폭 겹침 ≥30). clamp_to_bounds, place_without_overlap, validate_layout, snap_panel.
- rot 규약: 도 CCW. rot=0일 때 panel_left=−x쪽, panel_right=+x쪽.

### services/scene.py — SceneState (구현 완료)
- 조회 environment()/furniture()/states()/recent(n). 편집 transform(snap)/move(x,y,rot)/store. transform/move 직후 자동 clamp.
- 버전 commit(전역 turn++, 절대 스냅샷, session.json) / commit_if_changed / revert_to.

## 13. 시각 자가검증 (VLM critic) — **제거됨**

v3의 VLM 시각 자가검증은 제거. 이유: 턴마다 렌더+VLM 호출이 도구 호출·지연 과다, HITL-2가 조화 확인 담당. 대체: (1) AGENT_PROMPT의 실행 전 자가 점검, (2) HITL-2 승인.

## 14. TODO (v4.3 우선순위 재편)

### 선행 조건 (이게 없으면 평가 층 전체가 안 돔)
1. **기능층 feasible=false 영구 기록** — 콘솔 로그가 아니라 session.json에. 원 발화·item·reason 포함. **T1 트리거의 전제.**
2. **조작 UI를 본 뷰어로 통합** — 16.1. baseline 별도 모듈 방향의 폐기.
3. **리플레이 재생 모드** — 16.2. C1 조건 자극 제시용.

### 로깅 확장
4. HITL-1/HITL-2에 제시 시각·응답 시각·응답 유형 기록
5. 뷰어 카메라 조작 이벤트 기록
6. 발화 원문을 intent와 분리 저장
7. 브라우저 이벤트에 발생 시각 포함 전송 → 서버 시각으로 단일화
8. UI 조작의 조작 묶음 단위 turn 커밋 (`source: "ui"`)
9. 프로빙 이벤트에 유발 원인 태그 (프로빙 유발 / 시스템 동작 유발)

### 평가 층 구현 (17절)
10. `services/metrics.py` — state → 기하 지표 (순수 함수)
11. `services/detectors.py` — T1~T9 결정론 탐지
12. `eval/normalize.py`, `eval/mapping.py`, `eval/probe.py`
13. `eval/retro.py` — 소급 계산 배치
14. `eval/codebook.json` — 이중 장부

### 기존 이월
15. find_placement 앵커 링 탐색(15°)의 사각지대 — ① 다중 링(+20cm씩) ② 조사 모드 폴백 ③ 각도 세분화(15°→7.5°). 전부 services/placement 내부 완결.

## 15. 논문 / 실험 방향 (v4.3 전면 개정)

### 15.1 논문 주제

**"사용자 행동으로부터 평가 기준을 생성하는 로보틱 가구 평가 프레임워크"**

핵심 주장: 로보틱 가구의 평가 기준은 연구자가 사전에 완결할 수 없다. 소수의 공통 요소를 씨앗으로 삼고, 사용자의 상호작용에서 발생하는 신호로 프레임워크 자체가 자라나게 할 수 있다.

기여는 "LLM으로 가구를 배치했다"가 아니라 **"평가 기준의 생산 주체를 연구자에서 사용자로 옮겼다"**이다. 시스템은 기여가 아니라 도구다.

### 15.2 씨앗 평가 요소 (선행연구 6편에서 3편 이상 반복된 것만)

| | 요소 | 문헌 근거 |
|---|---|---|
| **M1** | **의도 가독성** — 가구의 다음 동작과 의도를 사용자가 읽어낼 수 있는가 | Mechanical Ottoman / KirigamiTable / coMotion |
| **M2** | **맥락 적합성** — 결과 배치가 상황의 활동·인원·관계에 맞는가 | TransformTable / Proxemic Transitions / coMotion / KirigamiTable |

- **M1은 고정 지표.** 측정 대상이 상호작용 프로토콜(HITL 게이트·되묻기·타이밍)이라 사용자가 새 항목을 붙일 여지가 없다.
- **M2는 열린 컨테이너.** 새 평가 요소의 대부분이 여기 붙는다. 이 비대칭 자체가 설계 주장이다.

#### 「행동 방해」를 씨앗에서 제외한 이유 (v4.4)

선행연구 세 편(TransformTable, coMotion, Mechanical Ottoman)이 반복 측정한 「방해」는 **로봇의 물리적 상태 변화가 사용자의 진행 중인 신체 활동·대화를 침범하는 것**이다.

- TransformTable: 변형에 20~40초가 걸리는 동안 참가자들이 물러나고 대화가 끊겼다 (대화 방해 3.4)
- coMotion: 앉아 있는 사람 밑에서 좌면이 30~40도 기울어졌다. 어지럼증 호소 사례
- Mechanical Ottoman: 빠르게 직진해 다가오면 위협적으로 지각

**시뮬레이션에서는 이것을 직접 관측할 수 없다.** 화면 안에 사람이 없고, 사용자는 화면 밖에서 배치를 조작하는 관찰자 위치이므로 동선이 막히거나 자세가 무너지는 것을 몸으로 겪지 않는다. 이 지점은 실물 실험의 영역이며 한계로 명시한다(18절).

**대신 두 갈래로 처리한다.**

1. **공간 침범 관련 상태량은 M2에 흡수한다** — 변형 전후 footprint 증가량, 통로 폭 감소량, 기존 가구·벽과의 최소 간격, 인원 대비 잔여 활동 면적. 사용자가 배치를 보고 「지나다니기 불편하겠다」고 판단하는 것은 곧 맥락 적합성 판단이다.
2. **사용자가 언어화하면 M2의 새 축 후보로 잡힌다** — "이렇게 놓으면 지나다니기 불편할 것 같은데"는 T3(평가 술어)에 걸려 후보로 등록된다. 시스템이 직접 못 재도, 사용자가 말하면 프레임워크가 잡아낸다.

#### 부수 지표 — 상호작용 마찰 (IF, Interaction Friction)

기존 M2의 로그 지표(개입·대기·포기)는 **물리적 방해가 아니라 소프트웨어 도구를 쓰는 마찰**을 잰다. 구성개념이 다르므로 씨앗에서 분리하고 별도 이름을 준다.

- 선행연구 세 편에는 근거가 없다. 그쪽 참가자들에게는 조작 인터페이스 자체가 없었다.
- 따라서 **씨앗 요소가 아니라 시스템 품질 지표**이며, NASA-TLX·신뢰·통제감과 같은 위치에 둔다.
- 사용자가 새 항목을 추가하는 대상이 아니다.

씨앗은 **연구자가 문헌에서 도출한 것**이며, 실험 시작 전 **사전 등록**한다. 그 밖의 모든 요소는 사용자에게서 나온다. 이 경계를 논문에 한 문장으로 선언한다.

### 15.3 실험 조건 (2조건, 참가자 간 설계)

| | 조작 | 실시간 프로빙 | 사후 설문·인터뷰 |
|---|---|---|---|
| **C1 기존 방식** | ✗ 리플레이 관찰 | ✗ | ○ |
| **C3 제안 방식** | ○ UI + 발화 혼합 | ○ | ○ |

- **C1**: 조작하지 않는다. 실제 C3 참가자 세션의 리플레이를 보고 think-aloud, 이후 설문·인터뷰. 연구자가 만든 구성이 아니라 실제 사용자 구성을 써야 "연구자가 이상한 걸 보여줬다"는 반론이 막힌다. 이 형식은 이 분야의 현행 표준이다(FrameLight는 사전 녹화 영상 온라인 제시 N=13, "Sit on me please"는 비디오 프로토타입 크라우드소싱).
- **C3**: UI 조작과 자연어 요청을 모두 사용. 트리거 발화 시 시스템이 설명→확인→근거 3문 프로빙.
- **C2(구 조건)는 삭제.** 그 역할은 **C3 내부 비교**로 대체한다 — 같은 참가자 안에서 프로빙으로 나온 기준과 사후 인터뷰로 나온 기준을 구분해 프로빙의 고유 기여를 분리한다.

**두 개의 비교**
- C1 vs C3 (참가자 간): 기존 평가 방식 대비 전체 효과. 논문의 헤드라인
- C3 내부: 프로빙 산출 vs 인터뷰 산출. 참여를 상수로 둔 상태에서 프로빙의 고유 기여

### 15.4 변수

- **독립변수**: 평가 방식 2수준
- **통제변수**: 조작 방식(C3에서 혼합 고정), scene(거실 고정), 기존 가구 고정, 초기 구성 동일, 물리 레이어 동일, 로봇 2대, 프로빙 예산 상한, 입력 해상도(이동 격자 스냅·회전 45°·패널 5단계), 시나리오 순서 균형배치
- **관찰변수**: 채널 배분 — 발화 유래 대 UI 유래 상태 변경 비율, 발화 직후 수동 미세조정 크기, 채널 이탈률. **조작하지 않고 측정만 한다.**
- **주 종속변수**: 참가자당 novel 평가 기준 수 / 발화의 M1·M2·novel 축별 분포 / 발화당 기준 밀도 / 관측 불가 축의 수와 내용
- **부 종속변수(C3만)**: M1 로그 6종, M2 기하 8종 + 판정 층 5종, 상호작용 마찰(IF) 로그 3종 + 자기보고 3문항, NASA-TLX, 신뢰·통제감, 완료 시간·조작 횟수

> **IF 자기보고 주의**: TransformTable의 3문항(놀랐다·혼란스러웠다·방해했다)을 차용하되 「변형」을 「시스템이 만든 변경」으로 문구 조정한다. 원 문항은 물리적 변형에 대한 반응을 물은 것이므로 **원 척도의 실측치와 직접 수치 비교는 하지 않는다.** v4.3까지는 비교 가능하다고 서술했으나 철회한다.

> 주 종속변수만이 두 조건 모두에서 측정된다. 나머지는 C3 내부 분석용이다. 이를 숨기지 않고 명시한다.

### 15.5 과제와 시나리오

거실 고정. 가구 이름을 포함하지 않는 활동 프롬프트.

| | 프롬프트 | 변주 |
|---|---|---|
| S1 | "혼자 노트북으로 일할 거야" | 1인 · 개인 작업 |
| S2 | "친구랑 둘이 차 마시면서 얘기할 거야" | 2인 · 대화 |
| S3 | "셋이 모여서 보드게임 할 거야" | 3인 · 공동 활동 |

인원이 늘수록 로봇 2대로 충족하기 어려워지도록 설계. S3에서 요구–능력 간극이 발생할 가능성이 높고 그 지점이 새 기준의 발생 지점이 된다.

**[미정] S3의 난이도** — 간극이 지나치게 노골적이면 인위적으로 보인다. 파일럿에서 조정.

### 15.6 절차

1. 동의·사전 설문 (5분)
2. 튜토리얼 (10분) — 연습 시나리오 1개(다른 방). **평가 기준 도출이 목적임은 고지하지 않음**
3. 본 과제 (각 10~12분 × 3) — 순서 균형배치, think-aloud 병행
4. 시나리오 간 설문 (각 2분) — TransformTable 3문항
5. 사후 설문 (8분) — NASA-TLX, 신뢰·통제감
6. 종료 인터뷰 (15분) — **두 조건 공통 문항**
7. 디브리핑 (5분)

**종료 인터뷰 공통 문항**
- 이 로봇 가구가 무엇이라고 이해하셨나요?
- 배치를 볼 때 무엇을 먼저 보셨나요?
- 마음에 들지 않았던 점이 있다면 무엇인가요?
- **이 가구가 할 수 없어서 아쉬웠던 것이 있나요?** ← C1에서 미충족 정보를 끌어내는 유일한 통로
- 무엇에 비유할 수 있을까요?

**[미정] C1의 관찰 시간 보정 방식** — 시간 연장인가 회고 구간 삽입인가.

### 15.7 표본과 순서 제약

- C1 12명, C3 12~16명. 총 24~28명. **[미정] 최종 표본과 조기 종료 기준** — 파일럿 후 포화 추이로 결정.
- **순서 제약**: C1의 리플레이 자극을 만들려면 C3 세션이 먼저 수집되어야 한다. 파일럿 → C3 일부 → 대표 세션 선정 → C1 자극 확정 → 나머지 무작위 배정.
- **[미정] 대표 세션 선정 기준.**

### 15.8 분석

- **사전 코드북 고정·사전 등록** — M1·M2의 최초 항목. "연구자가 정한 것은 이것뿐"임을 증명 가능하게
- novel 판정: 코더 2인 독립 코딩 + Cohen's κ. **판정은 user_verbatim만으로**
- 산출 경로 구분(C3): 프로빙에서만 / 인터뷰에서만 / 양쪽 모두
- 중복 스크리닝: 소급 계산 후 기존 지표와 상관 산출. **[미정] 임계값과 코더 판정 절차**
- 축별 분포 비교, 포화 곡선
- 능력 경계 분석: novel 기준이 12절 경계 목록 근처에 몰리는가. **예측을 사전 등록**
- **[미정] 사후 타당화 세션 설계** — 도출된 목록을 별도 집단에 제시해 중요도 평정

### 15.9 선행연구 축

1. 로보틱 가구 6편(본 연구의 씨앗 도출 대상): Mechanical Ottoman(HRI'15), coMotion(TEI'14), TransformTable(ITS'13), Proxemic Transitions(CHI'17), KirigamiTable(CHI'20), Drift Table(CHI'04)
2. 형태 변화 인터페이스 이론: Rasmussen et al.(CHI'12) 설계공간 리뷰, Rasmussen et al.(TEI'16) 통제 배분 4단계
3. 로보틱 가구 계보: Furnituroid(HRI'22, HRI'24), FrameLight(HRI'25), CollaBot(HRI'24)
4. Adaptive Architecture: Nguyen et al.(HRI'25) architectonic gestures, Nguyen & Vande Moere(CHI'24) 공유 통제
5. 사용자 조작 UI 선례: Web-Based Multi-Robot Furniture UI(HRI'21)
6. 지표·구조 참조: Steinfeld(HRI'06), VoicePilot(UIST'24)

## 16. 뷰어 확장 (v4.3 신규 — baseline 분리를 대체)

### 16.1 조작 UI 통합

v4.2는 조작 UI를 LLM 없는 별도 `baseline/` 모듈에 두기로 했다. **v4.3에서 이 방향을 폐기한다.** 본 실험에 수동 전용 조건이 없고 C3가 UI와 발화를 함께 쓰므로, 분리가 아니라 **통합**이 필요하다.

**조작 방식 (확정)**
- **이동**: 로봇/가구 드래그 + **격자 스냅**. 회전(45° 스텝)과 패널(5단계)이 이미 이산이므로 이동도 이산화해야 "거리를 3회, 각도를 5회 조정"의 단위가 맞는다.
  - **[미정] 격자 크기** — 방 크기와 로봇 치수 기준으로 후보를 정한 뒤 파일럿에서 불편 언급 여부 확인
- **회전**: 선택 시 뜨는 회전 버튼(↺↻), 45° 스텝
- **패널**: 선택된 로봇에 좌/우 각각 5단계 버튼(0/45/90/135/180). 가구엔 안 뜸
- **선택/제거**: 클릭 선택, X로 도크 복귀

**혼용 운영 규칙**
1. **배타적 잠금** — LLM 파이프라인 실행 중 UI 잠금. 단 **잠금 중 조작 시도는 거부하되 기록**한다. 이 기록이 상호작용 마찰(IF)의 「대기 중 조작 시도」 지표다.
2. **조작 묶음 단위 커밋** — 마지막 조작 후 2초 무입력이면 하나의 turn으로 확정. `source: "ui"`.
3. **채널 전환 기록** — 발화→UI 전환 시점과 직전 사건(HITL 거부 / 되묻기 / 긴 대기)을 기록. 지표명 **채널 이탈률**.
4. **물리 판정은 파이썬** — 브라우저는 조작 의도를 상향 전송하고, collision.py가 판정한 결과를 받아 그린다. 브라우저에서 독자 판정하지 않는다.

### 16.2 리플레이 재생 모드

C1 조건의 자극 제시용. 현재 기능 없음.

- 입력: 특정 세션의 `session.json` (커밋 스냅샷 시퀀스 + 이벤트 타임라인)
- 동작: 스냅샷 간 전이를 실제 소요 시간에 맞춰 재생. 로봇 이동·패널 변형이 애니메이션으로 보여야 한다
- 제약: 참가자는 조작할 수 없다. 카메라 조작만 허용(그리고 그것도 기록한다)
- 발화·프로빙 말풍선은 **표시하지 않는다.** C1은 구성 과정만 관찰하는 조건이며, 원본 참가자의 발화를 보여주면 그 사람의 평가 기준이 노출된다

## 17. 평가 층 (v4.3 신규)

구성 파이프라인(1~9단계)의 동작을 **바꾸지 않고** 관측과 질문만 추가한다. 평가 층을 꺼도 시스템은 정상 동작해야 한다.

### 17.1 트랙 구분

| 구분 | 단계 | 시점 |
|---|---|---|
| **파생 지표 계산** | 트랙이 아니라 **순수 함수** | `state → 숫자`. 프로빙 문구 삽입 / 세션 분석 / 소급 계산 세 곳에서 동일 호출 |
| **실시간** | 이벤트 수집 → 후보 탐지 → 후보 정규화 → 확인 프로빙 | 수 초 내. 정규화는 LLM이라 **비동기**로 돌리고, 지연이 임계를 넘으면 그 후보는 프로빙을 건너뛴다(데이터로는 남음) |
| **사후** | 측정 가능성 판정 → 소급 계산 | 시스템 자동 배치. **연구자 판단 개입 없음.** 새 지표 확정 시 트리거 |

### 17.2 후보 탐지 (결정론, LLM 없음)

LLM을 쓰면 "무엇을 평가로 볼지"를 LLM이 정하게 되어 논문 주장이 무너진다. 규칙표를 논문에 그대로 게재한다.

| | 트리거 | 규칙 |
|---|---|---|
| T1 | 미충족 요청 | 기능층 `feasible=false`. 원 발화·item·reason 함께 기록 |
| T2 | 정도 표현 | 비교급·정도 부사 — "더 ~", "덜 ~", "너무 ~", "충분히", "조금만" |
| T3 | 평가 술어 | 평가 형용사 — "괜찮", "이상", "불편", "어색", "별로" |
| T4 | 능력 질문 | 질문형 + 가능성 — "~할 수 있어?", "~는 안 돼?" |
| T6 | 반복 후 이탈 | 유사 취지 발화 2회 이상 후 화제 전환 |
| T7 | 축 반복 조정 | 동일 축을 한 조작 묶음 안에서 N회 이상 (UI·발화 공통) |
| T8 | 한계 도달 | 조정값이 경계 도달 — 패널 180°에서 추가 입력, 벽면 밀착 상태에서 추가 드래그. **feasible=false의 조작판** |
| T9 | 되돌림 진동 | 조정 → 원복 → 재조정 반복 |

**보조 태그 (트리거 아님)** — 「속성 수정」: `intent_type=modify` ∧ furniture 목록이 직전 턴과 동일. 사물이 아니라 속성을 고치려는 요청. 원래 T5 트리거였으나 "좀 옆으로 옮겨줘" 같은 일상적 재배치까지 걸려 프로빙 큐를 오염시키므로 **태그로 강등**. T7의 입력과 M2 판정 층의 「반복 조정된 파라미터」 산출에 쓴다.

**[미정] 탐지 임계값** — 즉시 revert의 N턴, 판독 지연의 배수, 축 반복 조정의 N회. 파일럿 3~5명으로 보정.
**[미정] T2·T3의 한국어 어휘 목록** — 말뭉치 검토 후 확정. 파일럿에서 미검출 사례 수집.

### 17.3 정규화 (LLM, 제한 출력)

같은 평가가 "의자가 더 컸으면", "이거 좀 작지 않나?", "덩치 큰 사람은 못 앉겠는데"처럼 제각각 표현된다. 고정 슬롯으로 정리해야 비교·집계·매핑이 가능하다.

```json
{
  "verbatim": "덩치 큰 사람은 못 앉겠는데",
  "attribute_user_words": "덩치",
  "direction": "increase",
  "what_should_change": "앉는 면이 더 넓어야 한다"
}
```

- `attribute_user_words`는 **원문에 substring으로 존재해야 통과.** 없으면 거부하고 재시도. LLM 조어 금지
- `what_should_change`에 **상태 변수명·기하 용어 사용 금지**
- LLM이 이 단계를 맡는 이유: "덩치 큰 사람은 못 앉겠는데"에서 `direction=increase`를 뽑으려면 언어 이해가 필요하다. 어휘 규칙은 "평가를 담고 있다"까지만 알려준다
- 정규화 실패(대명사만 있는 발화 등)는 그 자체를 프로빙 트리거로 처리한다

### 17.4 측정 가능성 판정과 계산식 생성 (LLM + 코드 검증)

| 판정 | 처리 |
|---|---|
| 계산 가능 | 계산식 생성 → M2 확장 슬롯에 잠정 등록 |
| 계산 불가 | 관측 불가 큐에 verbatim + 발생 맥락 적재. 정성 결과로 보고 (재질·색·질감·온도·소리 등) |

**계산식 생성을 연구자가 아니라 LLM이 담당한다.** 본 연구의 목적이 사용자 관점의 평가 요소를 추출하는 것이므로, 연구자 판단이 매핑에 개입하면 그 자체가 오염이다. 어느 계산식을 고르느냐에 따라 숫자와 결론이 달라진다.

이 목적에 한해 LLM이 낫다: (1) 연구자는 가설을 알아 무의식적으로 흥미로운 결과 쪽으로 매핑할 수 있으나 LLM은 가설을 모른다, (2) 참가자가 늘어도 판단 기준이 흔들리지 않는다, (3) 모든 매핑을 로그로 남겨 전수 공개할 수 있다.

**검증 — 판단이 아니라 자동 검사**
- **스키마 검증**: 계산식이 화이트리스트 상태 변수(`x, y, rot, panel_left, panel_right, 연결 관계, scene 가구 bbox`)만 참조해야 통과. 위반 시 **자동으로 측정 불가로 분류**. 문법 검사이며 판단이 아니다
- **실행 검증**: 샘플 상태 집합에 실행. 오류 발생 또는 상수만 반환하면 거부
- **일관성 검증**: 동일 발화에 N회 반복 실행해 일치율 산출, 논문에 보고
- **전수 공개**: 모든 매핑을 「사용자 표현 → 계산식」 쌍으로 부록 게재. 코더는 **감사**하여 오류율만 보고하며 계산식을 작성·수정하지 않는다

### 17.5 확정 프로빙과 소급 계산

**프로빙 4단계** — 탐지(결정론) → 설명(투명성) → 확인(사용자 verbatim) → 근거("왜 중요한가")

관측 사실을 문구에 넣는다. "두 로봇 사이 거리를 세 번 조정하셨는데"는 로그에서 확정된 사실이지 추측이 아니므로 유도 편향에서 자유롭다.

**예산 관리**
- 세션당 상한 / 같은 축 반복 프로빙 금지(누적 코드북 참조) / 직전 프로빙 후 최소 간격 / 연속 조작 중인 몰입 구간에는 대기
- **[미정] 예산 상한, 최소 간격, 질문 문구 세트** — 파일럿 + 유도 편향 검토

**소급 계산** — 확정된 지표는 정의 시점 이후가 아니라 **데이터셋 전체**에 적용한다. history가 절대 스냅샷이므로 가능하다. P7의 발화에서 나온 축을 P1~P6에도 계산하면, 「앞선 참가자들도 같은 조건이었으나 언어화하지 못했다」를 확인할 수 있고 이것이 논문의 핵심 증거가 된다.

**코드북 이중 장부**

| 용도 | 운용 | 이유 |
|---|---|---|
| 분석용 | **고정** — M1·M2의 최초 항목만 | novel 판정 기준이 참가자마다 달라지면 순서가 결과를 오염시킨다 |
| 시스템 동작용 | **누적** — 확정된 새 지표를 계속 추가 | 이미 도출된 기준을 반복해 캐묻지 않도록 프로빙 우선순위 조정 |

### 17.6 로그 스키마 (session.json 확장)

```json
{
  "session": "p07_livingroom_s3",
  "condition": "C3",
  "task": "셋이 모여서 보드게임 할 거야",
  "participant": {"id": "p07"},
  "events": [
    {"t": 12.4, "type": "utterance", "text": "의자가 더 컸으면 좋겠어"},
    {"t": 12.9, "type": "intent", "payload": {"...12필드..."}},
    {"t": 13.1, "type": "hitl1_shown"},
    {"t": 19.7, "type": "hitl1_response", "value": "approve", "latency": 6.6},
    {"t": 20.2, "type": "function", "item": "의자", "feasible": false,
     "reason": "본체 크기 변경 불가", "source_utterance": "의자가 더 컸으면 좋겠어"},
    {"t": 20.4, "type": "trigger", "ids": ["T1", "T2"]},
    {"t": 21.8, "type": "probe", "stage": "explain", "text": "..."},
    {"t": 24.0, "type": "probe_answer", "text": "덩치 큰 사람도 앉아야 하니까"},
    {"t": 31.5, "type": "ui_action", "action": "move", "obj": "BOT 1", "to": [120, 300]},
    {"t": 40.0, "type": "camera", "action": "orbit"},
    {"t": 44.2, "type": "hitl2_response", "value": "reject"},
    {"t": 51.0, "type": "commit", "turn": 7, "source": "utterance"},
    {"t": 58.3, "type": "revert", "to_turn": 6}
  ],
  "criteria": [
    {
      "id": "x01",
      "user_verbatim": ["의자가 더 컸으면 좋겠어", "덩치 큰 사람도 앉아야 하니까"],
      "trigger": ["T1", "T2"],
      "context": {"turn": 7, "scenario": "S3"},
      "attribute_user_words": "컸으면",
      "formula": "...",
      "system_label": "...",
      "validation": {"schema": "pass", "exec": "pass", "consistency": 0.0},
      "status": "confirmed"
    }
  ],
  "unobservable": [
    {"user_verbatim": "이 방이 좀 답답한데", "trigger": ["T3"], "context": {"turn": 9}}
  ],
  "commits": [{"turn": 1, "state": {}}],
  "duration": 0.0
}
```

**필드별 생성 주체 (논문 부록에 그대로 게재)**

| 필드 | 생성 주체 |
|---|---|
| `user_verbatim` | **사용자** — novel 판정의 유일한 근거 |
| `trigger`, `context`, `events` | 코드 |
| `attribute_user_words` | LLM (추출, substring 검증) |
| `formula`, `system_label` | LLM (생성, 3중 검증). **novel 판정에 사용 금지** |

### 17.7 품질 관리

- 시각 기준 단일화: 브라우저 이벤트는 수신 시각이 아니라 **발생 시각**을 함께 보내 서버 시각으로 정렬. 어긋나면 모든 지표가 무효
- 프로빙 유발 방해와 시스템 동작 유발 방해를 이벤트 태그로 분리 집계
- 최소 조작 수·최소 시간 미달 세션 제외, 중복 참가 차단

## 18. 미정 항목 모음

구현 또는 실험 전에 결정해야 하는 것들. 각 항목의 결정 방법을 함께 적는다.

| | 항목 | 결정 방법 |
|---|---|---|
| 1 | 탐지 규칙 임계값 (revert N턴, 판독 지연 배수, 축 반복 N회) | 파일럿 3~5명 |
| 2 | 프로빙 예산 상한·최소 간격·질문 문구 세트 | 파일럿 + 유도 편향 검토 |
| 3 | T2·T3의 한국어 어휘 목록 | 말뭉치 검토 + 파일럿 미검출 사례 수집 |
| 4 | 격자 스냅 크기 | 방·로봇 치수 기준 후보 → 파일럿 |
| 5 | 중복 스크리닝 상관 임계값과 코더 판정 절차 | 파일럿 데이터 분포 확인 |
| 6 | `get_recent_context`에 UI 조작 turn 포함 여부 | **코드 확인 필요** |
| 7 | C1 리플레이 대표 세션 선정 기준 | C3 초기 세션 수집 후. 사전 문서화 |
| 8 | C1 관찰 시간 보정 방식 (시간 연장 vs 회고 구간) | 파일럿 |
| 9 | 시나리오 S3 난이도 | 파일럿 |
| 10 | 사후 타당화 세션 설계 (표본·제시 방식·평정 척도) | 본 실험 결과 규모에 따라 |
| 11 | 최종 표본 크기와 조기 종료 기준 | 파일럿 후 포화 추이 |
| 12 | 리플레이에서 카메라 조작 허용 범위 | 파일럿 |
| 13 | M2에 흡수할 공간 침범 지표의 구체 목록 — 변형 전후 footprint 증가량의 산출식 | 구현 시 확정 |
| 14 | IF 자기보고 3문항의 최종 문구 (TransformTable 원문에서 조정) | 파일럿 |
| 15 | 물리적 방해를 다루는 실물 후속 연구의 범위 | 본 논문 이후
